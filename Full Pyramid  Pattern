#include<stdio.h>
void main()
{
    int r,c,n,k;
    printf("enter n:");
    scanf("%d",&n);
    for(r=1;r<=n;r++)
    {
        for(k=r;k<=n-1;k++)
        {
        printf(" ");
        }
    for(c=1;c<=(2*r-1);c++)
    {
        printf("*");
    }
    printf("\n");
    }
}
